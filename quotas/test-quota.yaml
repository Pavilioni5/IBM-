apiVersion: v1
kind: ResourceQuota

metadata:
  name: test-quota
  namespace: test

spec:
  hard:
    pods: "3"
    requests.cpu: "250m"
    requests.memory: "256Mi"
    limits.cpu: "500m"
    limits.memory: "512Mi"
